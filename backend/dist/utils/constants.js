/**
 * Constants used across the application
 */
// Fields to select when populating User references
export const USER_POPULATE_FIELDS = "user_name display_name avatarURL bio status lastSeen";
// Fields to select when populating User references (minimal)
export const USER_POPULATE_FIELDS_MINIMAL = "user_name display_name avatarURL status lastSeen";
